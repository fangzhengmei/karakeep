# Karakeep 网页抓取任务调度与执行机制深度分析

## 一、系统架构总览

Karakeep 采用基于 Restate 的分布式任务调度系统，通过插件化的队列机制实现网页抓取任务的全流程管理。整个系统由以下核心组件构成：

| 层级 | 组件 | 主要职责 | 关键文件 |
|------|------|---------|---------|
| 任务入口 | tRPC Router | 接收用户请求，创建抓取任务 | `packages/trpc/routers/bookmarks.ts` |
| 队列定义 | Shared Server | 定义队列类型、优先级配置 | `packages/shared-server/src/queues.ts` |
| 队列插件 | Queue-Restate | 基于 Restate 的队列实现（调度、分发） | `packages/plugins/queue-restate/src/` |
| 执行单元 | Crawler Worker | 实际执行网页抓取任务 | `apps/workers/workers/crawlerWorker.ts` |
| 状态追踪 | Event Log + Tracing | 任务状态追踪与可观测性 | `packages/shared-server/src/eventLogTypes.ts` |

---

## 二、调度入口：任务创建与入队

### 2.1 任务触发点

任务创建主要发生在书签创建流程中，位于 `packages/trpc/routers/bookmarks.ts:394-406`。但在此之前，有一个关键的**链接去重检查**决定了是否真的需要创建记录和入队：

```typescript
// packages/trpc/routers/bookmarks.ts:217-227
if (input.type == BookmarkTypes.LINK) {
  const alreadyExists = await attemptToDedupLink(ctx, input.url);
  if (alreadyExists) {
    return { ...alreadyExists, alreadyExists: true };  // 早返回，不创建新记录
  }
}
// ... 只有去重未命中才会继续执行后续的 insert + enqueue
```

**链接去重命中后的完整影响链**：

```
attemptToDedupLink() 命中
  ↓
return { ...alreadyExists, alreadyExists: true }  ← 早返回
  ↓
❌ 不执行 db.transaction（不插入 bookmarks / bookmarkLinks 记录）
❌ 不执行 crawlerQueue.enqueue（不创建抓取任务）
❌ 不触发 RuleEngine / searchReindex / Webhook
✅ 返回已存在的书签对象，客户端可通过 alreadyExists=true 判断
```

#### 2.1.1 链接去重函数 attemptToDedupLink

`packages/trpc/routers/bookmarks.ts:108-123`

```typescript
async function attemptToDedupLink(ctx: AuthedContext, url: string) {
  const result = await ctx.db
    .select({ id: bookmarkLinks.id })
    .from(bookmarkLinks)
    .leftJoin(bookmarks, eq(bookmarks.id, bookmarkLinks.id))
    .where(and(eq(bookmarkLinks.url, url), eq(bookmarks.userId, ctx.user.id)));

  if (result.length == 0) {
    return null;  // 未命中去重，继续创建流程
  }
  return (
    await Bookmark.fromId(ctx, result[0].id, /* includeContent: */ false)
  ).asZBookmark();  // 命中去重，返回已有书签
}
```

**去重判定条件**：
- `bookmarkLinks.url = 输入URL`（精确匹配，不含归一化）
- `bookmarks.userId = 当前用户ID`
- 两个条件**同时满足**才判定为重复

**关键特性**：
1. **用户维度隔离**：不同用户添加相同 URL 不会被去重
2. **精确 URL 匹配**：`http://example.com` 与 `https://example.com` 视为不同
3. **非原子性**：代码注释明确指出 "doesn't 100% protect from duplicates because of races"——并发请求可能绕过去重

### 2.2 双队列设计

系统设计了两个独立的抓取队列，避免低优先级任务影响用户体验：

| 队列名称 | 优先级 | 适用场景 | 重试次数 |
|---------|--------|---------|---------|
| `LinkCrawlerQueue` | Default (0) | 用户主动添加的书签 | 5次 |
| `LowPriorityCrawlerQueue` | Low (50) | 批量导入、后台任务 | 5次 |

**关键设计点：**
- 优先级数值越小，优先级越高（与 Liteque 语义一致）
- 通过 `shouldUseLowPriorityQueues()` 动态判断是否使用低优先级队列
- `groupId` 设为用户 ID，实现按用户的公平调度

### 2.3 幂等性机制：主流程与后台补抓的关键差异

通过 `buildCrawlIdempotencyKey()` 函数生成幂等键，但**是否生效取决于入队场景**：

```typescript
// packages/shared-server/src/queues.ts:115-117
export function buildCrawlIdempotencyKey(payload: ZCrawlLinkRequest): string {
  return `crawl:${JSON.stringify(payload, Object.keys(payload).sort())}`;
}
```

#### 2.3.1 四种入队场景对比

| 入队场景 | 是否传递 idempotencyKey | 代码位置 | 重复任务行为 |
|---------|------------------------|---------|------------|
| **主书签创建** | ❌ **不传递** | `bookmarks.ts:394-406` | 每次创建都会生成新任务 |
| **用户手动重抓** | ✅ **传递** | `bookmarks.ts:717-726` | 相同 payload 重复调用会被去重 |
| **管理员批量重抓** | ✅ **传递** | `admin.ts:237-244` | 相同 payload 重复调用会被去重 |
| **管理员单条重抓** | ✅ **传递** | `admin.ts:678-683` | 相同 payload 重复调用会被去重 |
| **规则引擎全页归档** | ✅ **传递** | `ruleEngine.ts:304-314` | 相同 payload 重复调用会被去重 |

**幂等键 payload 字段差异（关键：不同字段 = 不同幂等键 = 不去重）**：

`ZCrawlLinkRequest` schema 定义（`queues.ts:81-86`）：
```typescript
zCrawlLinkRequestSchema = z.object({
  bookmarkId: z.string(),
  runInference: z.boolean().optional(),
  archiveFullPage: z.boolean().optional().default(false),
  storePdf: z.boolean().optional().default(false),
});
```

| 入口 | payload 内容 | 生成的幂等键示例 |
|------|------------|---------------|
| **主书签创建** | `{ bookmarkId }` | ❌ 无幂等键 |
| **用户手动重抓** | `{ bookmarkId, archiveFullPage, storePdf }` | `crawl:{"archiveFullPage":false,"bookmarkId":"xxx","storePdf":false}` |
| **管理员批量重抓** | `{ bookmarkId, runInference }` | `crawl:{"bookmarkId":"xxx","runInference":true}` |
| **管理员单条重抓** | `{ bookmarkId }` | `crawl:{"bookmarkId":"xxx"}` |
| **规则引擎全页归档** | `{ bookmarkId, archiveFullPage: true, runInference: false }` | `crawl:{"archiveFullPage":true,"bookmarkId":"xxx","runInference":false}` |

**重要推论**：
- 同一书签从"用户手动重抓"和"规则引擎全页归档"入队，因 payload 不同（`storePdf` vs `runInference`），**幂等键不同，不会被互相去重**
- 同一书签从"管理员批量重抓"和"管理员单条重抓"入队，因前者含 `runInference` 字段，**幂等键不同，不会被互相去重**
- 管理员先批量重抓（runInference=true），再手动单条重抓（只有 bookmarkId），会**生成两个独立的抓取任务**

**主书签创建流程（无幂等键）：**
```typescript
// packages/trpc/routers/bookmarks.ts:386-406
const enqueueOpts: EnqueueOptions = {
  priority: shouldUseLowPriority ? QueuePriority.Low : QueuePriority.Default,
  groupId: ctx.user.id,
  // 注意：此处未设置 idempotencyKey！
};
await crawlerQueue.enqueue({ bookmarkId: bookmark.id }, enqueueOpts);
```

**用户手动重抓流程（有幂等键）：**
```typescript
// packages/trpc/routers/bookmarks.ts:717-726
const payload = {
  bookmarkId: input.bookmarkId,
  archiveFullPage: input.archiveFullPage,
  storePdf: input.storePdf,
};
await LowPriorityCrawlerQueue.enqueue(payload, {
  groupId: ctx.user.id,
  priority: QueuePriority.Low,
  idempotencyKey: buildCrawlIdempotencyKey(payload),  // 显式传递
});
```

#### 2.3.2 幂等键检查点与生命周期

**检查位置**：`packages/plugins/queue-restate/src/semaphore.ts:279-285`
```typescript
// Idempotency check - 仅在 idempotencyKey 存在时执行
if (req.idempotencyKey) {
  const exists = await ctx.get<boolean>(idemKey(req.idempotencyKey));
  if (exists) {
    return false;  // 重复任务，跳过
  }
}
```

**生命周期**：
1. **写入**：入队时写入 Restate KV（`semaphore.ts:330-332`）
2. **清除**：任务出队执行时清除（`semaphore.ts:189-191`）
3. **持久化**：由 Restate 状态存储管理，随队列状态持久化

---

## 三、队列分发机制：Dispatcher 与 Semaphore

### 3.1 Dispatcher 核心流程

`packages/plugins/queue-restate/src/dispatcher.ts` 实现了任务分发的核心逻辑：

**分发循环：**
```typescript
while (runNumber <= NUM_RETRIES) {
  // 1. 获取信号量（并发控制）
  const leaseId = await semaphore.acquire(priority, groupId, idempotencyKey);
  
  // 2. 调用 Runner 执行任务
  const result = await runner.run(jobData);
  
  // 3. 根据结果处理
  if (result.type === "rate_limit") {
    // 限流：释放信号量 + 睡眠重试（不消耗重试次数）
    await semaphore.release(leaseId);
    await ctx.sleep(result.delayMs);
    continue;
  }
  if (result.type === "error") {
    // 错误：触发 onError 回调 + 释放 + 重试
    await runner.onError({ job, error });
    await semaphore.release(leaseId);
    runNumber++;
    continue;
  }
  // 成功：触发 onComplete + 释放 + 退出循环
  await runner.onCompleted({ job, result });
  await semaphore.release(leaseId);
  break;
}
```

### 3.2 优先级队列 Semaphore 实现

`packages/plugins/queue-restate/src/semaphore.ts` 实现了基于优先级的并发控制：

**核心数据结构：**
```typescript
interface SemaphoreMetadata {
  paused: boolean;
  leases: Record<string, number>;  // 租约ID -> 过期时间
  groups: Record<string, GroupMeta>;  // 按组管理
}

interface GroupMeta {
  bestPriority: number;           // 组内最高优先级
  lastServedTimestamp: number;    // 上次服务时间（公平性）
  totalItemCount: number;         // 组内总任务数
  priorities: Record<number, PriorityQueueMeta>;  // 各优先级队列
}
```

**调度算法（selectAndPopItem）：**
1. **组间公平调度**：优先选择优先级最高的组；优先级相同时，选择上次服务最早的组
2. **组内按优先级**：同一组内按优先级数值从小到大执行
3. **先进先出**：同一优先级内按入队顺序执行

---

## 四、执行单元：Crawler Worker

### 4.1 Worker 初始化

`apps/workers/workers/crawlerWorker.ts:330-451` 定义了 Worker 的构建：

```typescript
static async build(queue: Queue<ZCrawlLinkRequest>) {
  await CrawlerWorker.ensureInitialized();
  
  const worker = (await getQueueClient()).createRunner(queue, {
    run: withWorkerTracing(
      "crawlerWorker.run",
      withWorkerEventLog("crawlerWorker.run", (job) =>
        runCrawler(job, queue.opts.defaultJobArgs.numRetries),
      ),
    ),
    onComplete: async (job) => {
      // 更新 crawlStatus 为 success
      await db.update(bookmarkLinks)
        .set({ crawlStatus: "success" })
        .where(eq(bookmarkLinks.id, bookmarkId));
    },
    onError: async (job) => {
      if (job.numRetriesLeft == 0) {
        // 彻底失败：更新状态 + 取消下游任务
        await db.transaction(async (tx) => {
          await tx.update(bookmarkLinks)
            .set({ crawlStatus: "failure" })
            .where(eq(bookmarkLinks.id, bookmarkId));
          await tx.update(bookmarks)
            .set({ taggingStatus: null, summarizationStatus: null })
            .where(and(eq(bookmarks.id, bookmarkId), ...));
        });
      }
    },
  }, {
    pollIntervalMs: 1000,
    timeoutSecs: serverConfig.crawler.jobTimeoutSec,
    concurrency: serverConfig.crawler.numWorkers,  // 并发数配置
  });
}
```

### 4.2 抓取主流程（runCrawler）

`apps/workers/workers/crawlerWorker.ts:2190-2353`

**执行步骤：**

1. **前置检查**
   - 参数校验（zCrawlLinkRequestSchema）
   - 获取书签详情（URL、已存在的资源等）
   - **域级限流检查**（checkDomainRateLimit）

2. **内容类型探测**
   - 发送 HEAD/GET 请求检测 Content-Type
   - PDF/图片类型直接转换为资产书签

3. **页面抓取（crawlPage）**
   - 浏览器模式：Playwright + Stealth 插件
   - 无浏览器模式：普通 HTTP 请求
   - 资源清理：page.close() + context.close() 均有超时保护

4. **HTML 解析子进程**
   - 通过 `execa` 启动独立子进程执行 metascraper
   - 内存限制：`--max-old-space-size` 参数控制
   - 超时保护：`parseTimeoutSec` 配置

5. **资源存储**
   - 截图（JPEG，80% 质量）
   - PDF（A4 格式）
   - 横幅图片
   - 整页归档（monolith）
   - HTML 内容（小于阈值直接存库，大于阈值存资产）

6. **触发下游任务**
   - AI 标签生成（OpenAIQueue）
   - AI 摘要生成（OpenAIQueue）
   - 搜索索引重建（triggerSearchReindex）
   - 视频下载（VideoWorkerQueue，可选）
   - Webhook 通知

---

## 五、并发与超时控制

### 5.1 并发控制层级

| 层级 | 控制机制 | 配置位置 |
|------|---------|---------|
| Worker 进程级 | `concurrency` 配置 | `serverConfig.crawler.numWorkers` |
| 浏览器实例级 | Mutex + 全局 Browser | `browserMutex`，单实例复用 |
| 域级限流 | RateLimit 服务 | `serverConfig.crawler.domainRatelimiting` |

### 5.2 超时保护矩阵

| 操作 | 超时时间 | 实现方式 |
|------|---------|---------|
| 任务整体 | `jobTimeoutSec` | Runner 配置 + AbortSignal |
| 页面导航 | `navigateTimeoutSec` | Playwright goto timeout |
| 网络空闲等待 | 5000ms | waitForLoadState("networkidle") |
| 截图捕获 | `screenshotTimeoutSec` | Promise.race + timeout |
| HTML 解析 | `parseTimeoutSec` | execa timeout |
| Page 关闭 | 5000ms | raceWith + timeoutRace |
| Context 关闭 | 10000ms | raceWith + timeoutRace |

**关键代码（超时信号）：**
```typescript
// packages/plugins/queue-restate/src/runner.ts:9-19
function timeoutSignal(timeoutSecs: number): AbortSignal {
  const controller = new AbortController();
  const timer = setTimeout(() => {
    controller.abort(new Error(`Job timed out after ${timeoutSecs} seconds`));
  }, timeoutSecs * 1000);
  return controller.signal;
}
```

### 5.3 资源泄漏防护

**Context 收割器（startContextReaper）：**
```typescript
// apps/workers/workers/crawlerWorker.ts:216-266
// 每 5 分钟检查一次，关闭超时的浏览器上下文
const maxContextAgeMs = (jobTimeoutSec + 30) * 1000 + 5 * 60_000;
setInterval(() => {
  for (const [id, entry] of activeContexts) {
    if (now - entry.createdAt > maxContextAgeMs) {
      // 强制关闭超时的 context
      raceWith(entry.context.close(), timeoutRace(10_000, () => false));
    }
  }
}, 5 * 60_000);
```

---

## 六、限流机制

### 6.1 域级限流

`apps/workers/workers/crawlerWorker.ts:2140-2188`

```typescript
async function checkDomainRateLimit(url: string, jobId: string): Promise<void> {
  const rateLimitResult = await rateLimitClient.checkRateLimit(
    {
      name: "domain-ratelimit",
      maxRequests: crawlerDomainRateLimitConfig.maxRequests,
      windowMs: crawlerDomainRateLimitConfig.windowMs,
    },
    hostname,
  );

  if (!rateLimitResult.allowed) {
    // 抖动策略：1.0 ~ 1.4 倍随机延迟，防止惊群效应
    const jitterFactor = 1.0 + Math.random() * 0.4;
    const delayMs = Math.floor(resetInSeconds * 1000 * jitterFactor);
    throw new QueueRetryAfterError(`Domain "${hostname}" is rate limited`, delayMs);
  }
}
```

**限流重试的特殊性：**
- 抛出 `QueueRetryAfterError` 而非普通 Error
- Dispatcher 收到后**不消耗重试次数**，仅睡眠后重试
- 实现：`dispatcher.ts:179-187`

---

## 七、重试机制与错误归因

### 7.1 重试计数标准：首次执行与重试的精确界限

**核心计数逻辑**位于 `packages/plugins/queue-restate/src/dispatcher.ts:92-93`：

```typescript
const NUM_RETRIES = queueOpts.defaultJobArgs.numRetries;  // 配置值 = 5
let runNumber = 0;
while (runNumber <= NUM_RETRIES) {  // 循环条件：<= 而非 <
```

#### 7.1.1 执行次数计数矩阵

| runNumber | 执行类型 | numRetriesLeft | 说明 |
|-----------|---------|----------------|------|
| 0 | **首次执行** | 5 | 第1次执行，还有5次重试机会 |
| 1 | **第1次重试** | 4 | 重试计数开始 |
| 2 | **第2次重试** | 3 | |
| 3 | **第3次重试** | 2 | |
| 4 | **第4次重试** | 1 | |
| 5 | **第5次重试** | 0 | 最后一次执行机会 |

**关键结论**：
- 配置的 `numRetries = 5` 表示：**1 次首次执行 + 5 次重试 = 总共 6 次执行机会**
- 当 `numRetriesLeft == 0` 时（即 runNumber == 5），标记为**彻底失败**

#### 7.1.2 重试策略配置

| 错误类型 | 最大重试次数 | 退避策略 | 消耗重试次数 |
|---------|------------|---------|------------|
| 业务错误（默认） | 5次 | 固定 1s 延迟 | ✅ 消耗 |
| RPC 服务不可用 | 5次 | 指数退避 + 完全抖动 | ✅ 消耗 |
| 403/429/5xx HTTP | 5次 | 由业务层触发重试 | ✅ 消耗 |
| 域级限流 | 无限制 | 服务端指定延迟 + 抖动 | ❌ 不消耗 |

**指数退避 + 完全抖动实现（RPC 错误）：**
```typescript
// packages/plugins/queue-restate/src/dispatcher.ts:170-172
const baseMs = Math.min(5000 * 2 ** runNumber, 60000);  // 上限 60s
const delayMs = Math.floor(ctx.rand.random() * baseMs);
await ctx.sleep(delayMs, "rpc error retry");
```

**退避时间序列示例**：
| runNumber | 基础延迟 | 随机范围 |
|-----------|---------|---------|
| 1 | 10s | 0 ~ 10s |
| 2 | 20s | 0 ~ 20s |
| 3 | 40s | 0 ~ 40s |
| 4 | 60s | 0 ~ 60s（上限） |
| 5 | 60s | 0 ~ 60s |

### 7.2 可重试 HTTP 状态码

`apps/workers/workers/crawlerWorker.ts:135-140`

```typescript
function shouldRetryCrawlStatusCode(statusCode: number | null): boolean {
  if (statusCode === null) return false;
  return statusCode === 403 || statusCode === 429 || statusCode >= 500;
}
```

### 7.3 错误类型分类

| 错误类型 | 处理方式 | 重试消耗 |
|---------|---------|---------|
| `QueueRetryAfterError` | 睡眠后重试 | 不消耗 |
| 403/429/5xx HTTP 状态 | 触发重试 | 消耗 1 次 |
| RPC 错误（服务不可用） | 指数退避重试 | 消耗 1 次 |
| 验证错误（Zod） | 直接标记完成 | 不重试 |
| 其他业务错误 | 1s 后重试 | 消耗 1 次 |

---

## 八、状态回写与失败追踪

### 8.1 数据库状态字段

`packages/db/schema.ts:249-277`

```sql
CREATE TABLE bookmarkLinks (
  id TEXT PRIMARY KEY,
  url TEXT NOT NULL,
  -- ... 元数据字段
  crawledAt TIMESTAMP,
  crawlStatus TEXT CHECK (crawlStatus IN ('pending', 'failure', 'success')) DEFAULT 'pending',
  crawlStatusCode INTEGER DEFAULT 200
);
```

### 8.2 成功路径状态流转

```
onComplete 回调 (crawlerWorker.ts:384-397)
  ↓
更新 bookmarkLinks.crawlStatus = 'success'
  ↓
触发下游任务（AI 标签、摘要、搜索索引）
```

### 8.3 失败路径状态流转

```
onError 回调 (crawlerWorker.ts:398-440)
  ↓
if (numRetriesLeft == 0)  // 彻底失败
  ↓
├─ 更新 bookmarkLinks.crawlStatus = 'failure'
├─ 清除 bookmarks.taggingStatus = null
├─ 清除 bookmarks.summarizationStatus = null
└─ 记录错误日志（含 stack trace）
```

### 8.4 可追踪线索

**1. 结构化事件日志（Event Log）**

`packages/shared-server/src/eventLogTypes.ts:1-9`

```typescript
{
  "event.name": "crawlerWorker.run",
  "job.id": "job_xxx",
  "job.run_number": 2,
  "crawler.url": "https://example.com",
  "crawler.domain": "example.com",
  "crawler.status_code": 503,
  "crawler.proxy": "http://proxy:8080"
}
```

**2. OpenTelemetry 分布式追踪**

通过 `withWorkerTracing` 和 `withSpan` 包装，每个任务生成完整的追踪链：
- `crawlerWorker.run` → 任务总览
- `crawlerWorker.crawlPage` → 页面抓取
  - `getBrowserInstance` → 浏览器实例获取
  - `createContext` → 上下文创建
  - `setupPage` → 页面配置
  - `validateNavigationTarget` → URL 验证
  - `navigate` → 页面导航
  - `waitForLoadState` → 等待加载
  - `captureAssets` → 资源捕获
    - `extractHtml` → HTML 提取
    - `captureScreenshot` → 截图
    - `capturePdf` → PDF
- `crawlerWorker.runParseSubprocess` → HTML 解析
- `crawlerWorker.storeScreenshot/Pdf` → 资源存储
- `crawlerWorker.downloadAndStoreFile` → 文件下载
- `crawlerWorker.archiveWebpage` → 整页归档

**3. Prometheus 指标**

- `crawlerStatusCodeCounter`：按状态码 + 代理统计
- `bookmarkCrawlLatencyHistogram`：首次抓取延迟分布
- `workerStatsCounter`：completed/failed/failed_permanent 计数

**4. Restate Journal**

Restate 原生保留 3 天的执行日志，可通过 Restate Dashboard 查看：
- 每次重试的输入输出
- 状态变迁历史
- 异常堆栈信息

### 8.5 链接去重与幂等键去重的系统对比

系统中存在**两种完全不同的去重机制**，分别在请求链路的不同阶段生效，容易混淆。以下从触发点、作用阶段和排查线索三个维度进行精确区分。

#### 8.5.1 两种去重机制的本质区别

| 维度 | 链接去重 (Link Dedup) | 幂等键去重 (Idempotency Key Dedup) |
|------|----------------------|-----------------------------------|
| **触发点** | `bookmarks.ts:219` — `attemptToDedupLink()` | `semaphore.ts:280-284` — 幂等键存在性检查 |
| **作用阶段** | **数据库写入之前**（请求入口层） | **队列入队之后、执行之前**（调度层） |
| **判断依据** | `bookmarkLinks.url` + `bookmarks.userId` 精确匹配 | `buildCrawlIdempotencyKey(payload)` 序列化后的字符串 |
| **生效范围** | 仅主书签创建流程 (`createBookmark`) | 仅后台补抓流程 (`recrawlBookmark` + admin 批量重抓) |
| **命中结果** | 返回已有书签，`alreadyExists: true` | Dispatcher 静默跳过，日志记录 "skipping ... existing idempotency key" |
| **对 DB 的影响** | 不插入任何新记录 | 记录已存在，只是不新增队列任务 |
| **对抓取任务的影响** | 不创建抓取任务 | 不创建重复的抓取任务 |
| **并发安全** | ❌ 非原子（代码注释承认有竞态窗口） | ✅ 原子（Restate 状态机保证） |
| **排查线索** | API 返回 `alreadyExists: true`；Event Log 中 `bookmark.already_existed: true` | Dispatcher 日志 "skipping due to existing idempotency key"；Restate Dashboard 中可查 |

#### 8.5.2 去重命中的完整请求链路对比

**链接去重命中的链路**（主书签创建）：

```
用户请求 createBookmark(url: "https://example.com")
  ↓
attemptToDedupLink(ctx, "https://example.com")  ← 触发点
  ↓ 查询 bookmarkLinks WHERE url = ? AND userId = ?
  ↓ 命中！
return { ...existingBookmark, alreadyExists: true }
  ↓
❌ 不插入 DB 记录
❌ 不入队抓取任务
❌ 不触发下游（AI/Webhook/索引）
✅ 客户端收到 alreadyExists=true
```

**幂等键去重命中的链路**（后台补抓）：

```
用户请求 recrawlBookmark(bookmarkId: "xxx", archiveFullPage: true)
  ↓
LowPriorityCrawlerQueue.enqueue(payload, { idempotencyKey: "crawl:{...}" })
  ↓
Restate Dispatcher 收到任务
  ↓
semaphore.acquire(priority, groupId, idempotencyKey)  ← 触发点
  ↓ 检查 Restate KV 中 idemKey("crawl:{...}") 是否存在
  ↓ 存在！
return false  → Dispatcher 跳过执行
  ↓
❌ 不创建新的队列执行
✅ DB 中已有书签记录（状态不变）
✅ Dispatcher 日志: "skipping ... existing idempotency key"
```

#### 8.5.3 链接去重为何不会新增记录和抓取任务

关键在于 `bookmarks.ts:217-227` 的**早返回（early return）模式**：

```typescript
if (input.type == BookmarkTypes.LINK) {
  const alreadyExists = await attemptToDedupLink(ctx, input.url);
  if (alreadyExists) {
    // 早返回！后续所有代码都不会执行
    return { ...alreadyExists, alreadyExists: true };
  }
}
// 以下代码只在去重未命中时执行：
// - ctx.db.transaction → 插入 bookmarks + bookmarkLinks
// - crawlerQueue.enqueue → 创建抓取任务
// - RuleEngine / Webhook / searchReindex 触发
```

这是**业务层的主动拦截**，不是队列层的去重。它发生在请求进入数据库事务之前，因此：
1. **数据库层面**：不会产生新的 `bookmarks` / `bookmarkLinks` 行
2. **队列层面**：不会产生新的抓取任务（因为 enqueue 代码根本不会被执行到）
3. **下游层面**：AI 标签、摘要、Webhook 均不会被触发

#### 8.5.4 幂等键去重为何不影响主流程

主书签创建流程 (`createBookmark`) **不传递 idempotencyKey**，这是故意的设计选择：

1. **链接去重已覆盖主流程**：如果同一用户添加相同 URL，`attemptToDedupLink` 会在更早的阶段拦截
2. **语义不同**：幂等键是"同一操作不要重复入队"，而主流程中每次创建都是独立的操作意图
3. **不同用户同 URL**：链接去重按 userId 隔离，不同用户添加相同 URL 不应被拦截——幂等键无法表达这种语义

#### 8.5.5 三个幂等键入口的详细对比

虽然三个入口都使用 `buildCrawlIdempotencyKey()`，但 payload 构造方式存在关键差异：

**入口1：用户手动重抓** (`bookmarks.ts:717-726`)
```typescript
const payload = {
  bookmarkId: input.bookmarkId,
  archiveFullPage: input.archiveFullPage,   // 用户可选
  storePdf: input.storePdf,                  // 用户可选
};
// groupId = ctx.user.id（按用户分组）
```

**入口2：管理员批量重抓** (`admin.ts:237-244`)
```typescript
const payload = {
  bookmarkId: b.id,
  runInference: input.runInference,          // 管理员可选
};
// groupId 未设置（无分组，全局共享）
```

**入口3：规则引擎全页归档** (`ruleEngine.ts:304-314`)
```typescript
const payload = {
  bookmarkId: this.bookmark.id,
  archiveFullPage: true,                     // 硬编码 true
  runInference: false,                       // 硬编码 false
};
// groupId = this.bookmark.userId（按书签所有者分组）
```

**相似点**：
- 都使用 `LowPriorityCrawlerQueue`，优先级为 `QueuePriority.Low`
- 都通过 `buildCrawlIdempotencyKey(payload)` 生成幂等键
- 幂等键的语义相同：相同 payload 串行化后的字符串一致即判定为重复

**不同之处**：

| 差异维度 | 用户手动重抓 | 管理员批量重抓 | 规则引擎全页归档 |
|---------|-----------|------------|-------------|
| payload 字段 | `bookmarkId + archiveFullPage + storePdf` | `bookmarkId + runInference` | `bookmarkId + archiveFullPage(true) + runInference(false)` |
| 缺失字段 | `runInference` | `archiveFullPage, storePdf` | `storePdf` |
| groupId | `ctx.user.id` | 未设置 | `this.bookmark.userId` |
| 参数来源 | 用户输入 | 管理员输入 | 硬编码 |
| 跨入口去重 | ❌ 不互相去重 | ❌ 不互相去重 | ❌ 不互相去重 |

**跨入口不去重的根本原因**：`buildCrawlIdempotencyKey` 严格按 payload 内容序列化，字段集合不同即生成不同的键。例如：
- `crawl:{"archiveFullPage":false,"bookmarkId":"x","storePdf":false}` ≠ `crawl:{"bookmarkId":"x","runInference":true}`
- 即使 bookmarkId 相同，不同入口构造的 payload 字段不同，幂等键必然不同

**对故障追踪的影响**：
- 同一书签可能同时存在多个不同入口的抓取任务，需通过 Restate invocationId 和 Event Log 中的 payload 区分来源
- 检查幂等键跳过日志时，需确认是同一入口的重复调用，而非不同入口的独立任务

#### 8.5.6 URL trim 与链接去重的关系

**代码中的关键时间差**：

```typescript
// 第1步：链接去重查询（使用原始输入 URL）
const alreadyExists = await attemptToDedupLink(ctx, input.url);  // ← 未 trim

// 第2步：DB 写入（对 URL 执行 trim）
url: input.url.trim(),  // ← trim 在此处发生
```

这意味着：
- **链接去重查询**使用的 `input.url` 是用户原始输入，**未经 trim**
- **数据库中存储的 URL** 是 `input.url.trim()`，**已 trim**
- 两步之间 URL 的值可能不同，导致去重判断与存储不一致

**具体影响场景**：

| 用户输入 | 去重查询目标 | DB 中存储的 URL | 能否命中已有记录 |
|---------|-----------|-------------|-------------|
| `"https://example.com"` | `"https://example.com"` | `"https://example.com"` | ✅ 正常匹配 |
| `"  https://example.com  "` | `"  https://example.com  "` | `"https://example.com"` | ❌ 去重查询用带空格的 URL，DB 中是无空格的 URL，查不到 |
| `"https://example.com"` | `"https://example.com"` | `"https://example.com"` | ✅ 第二次添加同 URL 时正常匹配 |

**注意**：Zod schema 中 `url: z.string().url()` 会验证 URL 格式合法性，但**不会执行 trim**。因此带前后空格的 URL（如 `" https://example.com "`）无法通过 `z.string().url()` 校验，实际上不太可能进入去重逻辑。但以下场景仍可能出现问题：

1. **URL 含尾部空格但校验通过**：某些 URL 规范化中间件可能在 Zod 校验后修改 URL
2. **updateBookmark 中的 URL trim**（`bookmarks.ts:472`）：用户更新书签 URL 时执行 `input.url.trim()`，如果更新后的 URL 与已有记录去重，trim 后的 URL 与 DB 中存储的一致，可以正确匹配

#### 8.5.7 故障排查实操指南

**排查"书签未创建"**：
1. 检查 API 返回中 `alreadyExists` 字段是否为 `true`
2. 检查 Event Log 中是否有 `bookmark.already_existed: true`
3. 确认 `bookmarkLinks.url` 是否精确匹配（注意 http vs https、尾部斜杠等）

**排查"重抓任务未执行"**：
1. 检查 Dispatcher 日志是否包含 "skipping ... existing idempotency key"
2. 检查 Restate Dashboard 中对应幂等键的 invocation 状态
3. 注意：幂等键在任务出队时即被清除（`semaphore.ts:189-191`），执行失败后再次重试入队会生成新任务

**排查"重复书签被创建"**（链接去重的竞态窗口）：
1. 检查两个 `createBookmark` 请求的时间戳是否非常接近
2. 代码注释承认 "doesn't 100% protect from duplicates because of races"
3. 并发请求可能在 `attemptToDedupLink` 查询和 `insert` 之间插入，导致两条记录均被创建

#### 8.5.8 容易误判的排查案例

**案例1：规则引擎与手动重抓同时触发，出现两次抓取**

```
现象：用户点击"重新抓取"，但规则引擎也配置了 downloadFullPageArchive 动作，
      同一书签生成了两个独立的抓取任务。

根因：两个入口的 payload 不同：
  - 手动重抓: { bookmarkId, archiveFullPage: false, storePdf: false }
  - 规则引擎: { bookmarkId, archiveFullPage: true, runInference: false }
  幂等键不同，不会互相去重。

排查：在 Event Log 中搜索 bookmarkId，查看是否有两条 crawlerWorker.run 记录，
      对比 payload 中 archiveFullPage 和 runInference 字段。
```

**案例2：管理员批量重抓后，单条重抓又触发了新任务**

```
现象：管理员执行批量重抓（runInference=true），随后对同一条记录执行单条重抓，
      该书签被抓取了两次。

根因：批量重抓 payload = { bookmarkId, runInference: true }
      单条重抓 payload = { bookmarkId }
      幂等键不同，互不去重。

排查：检查 Dispatcher 日志，确认两次入队的 payload 差异。
修复：管理员单条重抓也传入 runInference 参数，保持与批量入口一致。
```

**案例3：URL 协议差异导致链接去重失效**

```
现象：用户先添加 http://example.com，再添加 https://example.com，
      创建了两条独立的书签记录和两个抓取任务。

根因：attemptToDedupLink 使用 eq(bookmarkLinks.url, url) 精确匹配，
      http 和 https 是不同的字符串，不会被去重。
      这不是 bug，而是设计选择：不同协议可能指向不同内容。

排查：检查 DB 中同一用户下 bookmarkLinks.url 的值，确认协议前缀差异。
```

**案例4：URL 尾部斜杠差异导致链接去重失效**

```
现象：用户添加 https://example.com/ 后，又添加 https://example.com
      （无尾部斜杠），创建了重复书签。

根因：attemptToDedupLink 不做 URL 归一化（无尾斜杠处理、无域名小写化），
      精确字符串匹配认为两者不同。

排查：对比 DB 中 bookmarkLinks.url 字段，关注尾斜杠、大小写差异。
修复：在 attemptToDedupLink 前增加 URL 归一化步骤，或在 DB 层面添加
      URL 归一化索引。
```

**案例5：幂等键生命周期导致的"任务丢失"误判**

```
现象：用户快速连击两次"重新抓取"按钮，第一次执行失败后，第二次的请求
      似乎没有生成新任务。

根因分析（时间线）：
  T1: 用户点击重抓 → payload 入队，幂等键写入 Semaphore KV
  T2: 用户再次点击 → payload 入队，Semaphore 检测到幂等键已存在 → 跳过
  T3: 第一次任务出队执行 → 幂等键被清除（semaphore.ts:189-191）
  T4: 第一次任务执行失败 → Dispatcher 进入重试循环（不重新入队）
  T5: Dispatcher 重试耗尽 → 标记失败

此时第二次的请求已被幂等键跳过（T2），不会被重新执行。
用户看到的现象是"只执行了一次就失败了"。

排查：检查 Dispatcher 日志中是否有 "skipping ... existing idempotency key" 记录。
      如有，说明第二次请求在 T2 被去重跳过。
修复：用户需在第一次失败后手动再次点击"重新抓取"（此时幂等键已清除，
      新请求不会被去重）。
```

---

## 九、对外通知机制

### 9.1 Webhook 触发

`apps/workers/workers/crawlerWorker.ts:2331-2339`

```typescript
const webhookService = new WebhooksService(db);
await webhookService.triggerWebhook(
  bookmarkId,
  "crawled",    // 操作类型
  userId,
  enqueueOpts,   // 继承优先级和 groupId
);
```

**触发时机：** 抓取成功后，在 `runCrawler` 函数末尾同步调用（异步入队）

### 9.2 下游任务级联

抓取成功自动触发的下游任务链：

```
抓取任务成功
  ├─ OpenAIQueue → 标签生成 (type: "tag")
  ├─ OpenAIQueue → 摘要生成 (type: "summarize")
  ├─ SearchIndexingQueue → 搜索索引重建
  ├─ VideoWorkerQueue → 视频下载（可选）
  ├─ WebhookQueue → Webhook 通知
  └─ AssetPreprocessingQueue → 资产预处理（PDF/图片类型）
```

---

## 十、关键设计总结

### 10.1 架构亮点

1. **插件化队列**：Queue 接口抽象，支持 Restate/Liteque 多种实现
2. **优先级 + 公平性**：组间轮询 + 组内优先级，避免单用户饿死
3. **多层超时**：从任务级到操作级的完整超时保护
4. **资源隔离**：HTML 解析在独立子进程，防止内存泄漏影响主进程
5. **可观测性**：Event Log + Tracing + Metrics 三位一体

### 10.2 失败处理哲学

- **尽早失败**：各阶段检查 abortSignal，快速终止
- **优雅降级**：截图/PDF 失败不影响主流程，只记录警告
- **状态一致性**：数据库事务保证 crawlStatus 与下游任务状态原子更新
- **可追溯性**：多维度追踪线索，便于事后排查

### 10.3 核心文件速查

| 功能模块 | 文件路径 |
|---------|---------|
| 队列定义 | `packages/shared-server/src/queues.ts` |
| 队列接口 | `packages/shared/queueing.ts` |
| Restate 分发器 | `packages/plugins/queue-restate/src/dispatcher.ts` |
| Restate 执行器 | `packages/plugins/queue-restate/src/runner.ts` |
| 优先级信号量 | `packages/plugins/queue-restate/src/semaphore.ts` |
| 抓取 Worker | `apps/workers/workers/crawlerWorker.ts` |
| Worker 入口 | `apps/workers/index.ts` |
| 任务入队 | `packages/trpc/routers/bookmarks.ts` |
| 管理员重抓 | `packages/trpc/routers/admin.ts` |
| 规则引擎 | `packages/trpc/lib/ruleEngine.ts` |
| 数据库 Schema | `packages/db/schema.ts` |
| 事件类型定义 | `packages/shared-server/src/eventLogTypes.ts` |

---

*本分析基于 Karakeep 项目源码，生成时间：2026-05-30*
