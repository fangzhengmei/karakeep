# Karakeep 网页抓取任务调度与执行机制深度分析

## 一、系统架构总览

Karakeep 采用基于 Restate 的分布式任务调度系统，通过插件化的队列机制实现网页抓取任务的全流程管理。整个系统由以下核心组件构成：

| 层级 | 组件 | 主要职责 | 关键文件 |
|------|------|---------|---------|
| 任务入口 | tRPC Router | 接收用户请求，创建抓取任务 | `packages/trpc/routers/bookmarks.ts` |
| 队列定义 | Shared Server | 定义队列类型、优先级配置 | `packages/shared-server/src/queues.ts` |
| 队列插件 | Queue-Restate | 基于 Restate 的队列实现（调度、分发） | `packages/plugins/queue-restate/src/` |
| 执行单元 | Crawler Worker | 实际执行网页抓取任务 | `apps/workers/workers/crawlerWorker.ts` |
| 状态追踪 | Event Log + Tracing | 任务状态追踪与可观测性 | `packages/shared-server/src/eventLogTypes.ts` |

---

## 二、调度入口：任务创建与入队

### 2.1 任务触发点

任务创建主要发生在书签创建流程中，位于 `packages/trpc/routers/bookmarks.ts:394-406`：

```typescript
switch (bookmark.content.type) {
  case BookmarkTypes.LINK: {
    const crawlerQueue = shouldUseLowPriority
      ? LowPriorityCrawlerQueue
      : LinkCrawlerQueue;
    await crawlerQueue.enqueue(
      { bookmarkId: bookmark.id },
      enqueueOpts  // 包含 priority 和 groupId
    );
    break;
  }
}
```

### 2.2 双队列设计

系统设计了两个独立的抓取队列，避免低优先级任务影响用户体验：

| 队列名称 | 优先级 | 适用场景 | 重试次数 |
|---------|--------|---------|---------|
| `LinkCrawlerQueue` | Default (0) | 用户主动添加的书签 | 5次 |
| `LowPriorityCrawlerQueue` | Low (50) | 批量导入、后台任务 | 5次 |

**关键设计点：**
- 优先级数值越小，优先级越高（与 Liteque 语义一致）
- 通过 `shouldUseLowPriorityQueues()` 动态判断是否使用低优先级队列
- `groupId` 设为用户 ID，实现按用户的公平调度

### 2.3 幂等性机制

通过 `buildCrawlIdempotencyKey()` 函数生成幂等键，防止重复调度：

```typescript
// packages/shared-server/src/queues.ts:115-117
export function buildCrawlIdempotencyKey(payload: ZCrawlLinkRequest): string {
  return `crawl:${JSON.stringify(payload, Object.keys(payload).sort())}`;
}
```

---

## 三、队列分发机制：Dispatcher 与 Semaphore

### 3.1 Dispatcher 核心流程

`packages/plugins/queue-restate/src/dispatcher.ts` 实现了任务分发的核心逻辑：

**分发循环：**
```typescript
while (runNumber <= NUM_RETRIES) {
  // 1. 获取信号量（并发控制）
  const leaseId = await semaphore.acquire(priority, groupId, idempotencyKey);
  
  // 2. 调用 Runner 执行任务
  const result = await runner.run(jobData);
  
  // 3. 根据结果处理
  if (result.type === "rate_limit") {
    // 限流：释放信号量 + 睡眠重试（不消耗重试次数）
    await semaphore.release(leaseId);
    await ctx.sleep(result.delayMs);
    continue;
  }
  if (result.type === "error") {
    // 错误：触发 onError 回调 + 释放 + 重试
    await runner.onError({ job, error });
    await semaphore.release(leaseId);
    runNumber++;
    continue;
  }
  // 成功：触发 onComplete + 释放 + 退出循环
  await runner.onCompleted({ job, result });
  await semaphore.release(leaseId);
  break;
}
```

### 3.2 优先级队列 Semaphore 实现

`packages/plugins/queue-restate/src/semaphore.ts` 实现了基于优先级的并发控制：

**核心数据结构：**
```typescript
interface SemaphoreMetadata {
  paused: boolean;
  leases: Record<string, number>;  // 租约ID -> 过期时间
  groups: Record<string, GroupMeta>;  // 按组管理
}

interface GroupMeta {
  bestPriority: number;           // 组内最高优先级
  lastServedTimestamp: number;    // 上次服务时间（公平性）
  totalItemCount: number;         // 组内总任务数
  priorities: Record<number, PriorityQueueMeta>;  // 各优先级队列
}
```

**调度算法（selectAndPopItem）：**
1. **组间公平调度**：优先选择优先级最高的组；优先级相同时，选择上次服务最早的组
2. **组内按优先级**：同一组内按优先级数值从小到大执行
3. **先进先出**：同一优先级内按入队顺序执行

---

## 四、执行单元：Crawler Worker

### 4.1 Worker 初始化

`apps/workers/workers/crawlerWorker.ts:330-451` 定义了 Worker 的构建：

```typescript
static async build(queue: Queue<ZCrawlLinkRequest>) {
  await CrawlerWorker.ensureInitialized();
  
  const worker = (await getQueueClient()).createRunner(queue, {
    run: withWorkerTracing(
      "crawlerWorker.run",
      withWorkerEventLog("crawlerWorker.run", (job) =>
        runCrawler(job, queue.opts.defaultJobArgs.numRetries),
      ),
    ),
    onComplete: async (job) => {
      // 更新 crawlStatus 为 success
      await db.update(bookmarkLinks)
        .set({ crawlStatus: "success" })
        .where(eq(bookmarkLinks.id, bookmarkId));
    },
    onError: async (job) => {
      if (job.numRetriesLeft == 0) {
        // 彻底失败：更新状态 + 取消下游任务
        await db.transaction(async (tx) => {
          await tx.update(bookmarkLinks)
            .set({ crawlStatus: "failure" })
            .where(eq(bookmarkLinks.id, bookmarkId));
          await tx.update(bookmarks)
            .set({ taggingStatus: null, summarizationStatus: null })
            .where(and(eq(bookmarks.id, bookmarkId), ...));
        });
      }
    },
  }, {
    pollIntervalMs: 1000,
    timeoutSecs: serverConfig.crawler.jobTimeoutSec,
    concurrency: serverConfig.crawler.numWorkers,  // 并发数配置
  });
}
```

### 4.2 抓取主流程（runCrawler）

`apps/workers/workers/crawlerWorker.ts:2190-2353`

**执行步骤：**

1. **前置检查**
   - 参数校验（zCrawlLinkRequestSchema）
   - 获取书签详情（URL、已存在的资源等）
   - **域级限流检查**（checkDomainRateLimit）

2. **内容类型探测**
   - 发送 HEAD/GET 请求检测 Content-Type
   - PDF/图片类型直接转换为资产书签

3. **页面抓取（crawlPage）**
   - 浏览器模式：Playwright + Stealth 插件
   - 无浏览器模式：普通 HTTP 请求
   - 资源清理：page.close() + context.close() 均有超时保护

4. **HTML 解析子进程**
   - 通过 `execa` 启动独立子进程执行 metascraper
   - 内存限制：`--max-old-space-size` 参数控制
   - 超时保护：`parseTimeoutSec` 配置

5. **资源存储**
   - 截图（JPEG，80% 质量）
   - PDF（A4 格式）
   - 横幅图片
   - 整页归档（monolith）
   - HTML 内容（小于阈值直接存库，大于阈值存资产）

6. **触发下游任务**
   - AI 标签生成（OpenAIQueue）
   - AI 摘要生成（OpenAIQueue）
   - 搜索索引重建（triggerSearchReindex）
   - 视频下载（VideoWorkerQueue，可选）
   - Webhook 通知

---

## 五、并发与超时控制

### 5.1 并发控制层级

| 层级 | 控制机制 | 配置位置 |
|------|---------|---------|
| Worker 进程级 | `concurrency` 配置 | `serverConfig.crawler.numWorkers` |
| 浏览器实例级 | Mutex + 全局 Browser | `browserMutex`，单实例复用 |
| 域级限流 | RateLimit 服务 | `serverConfig.crawler.domainRatelimiting` |

### 5.2 超时保护矩阵

| 操作 | 超时时间 | 实现方式 |
|------|---------|---------|
| 任务整体 | `jobTimeoutSec` | Runner 配置 + AbortSignal |
| 页面导航 | `navigateTimeoutSec` | Playwright goto timeout |
| 网络空闲等待 | 5000ms | waitForLoadState("networkidle") |
| 截图捕获 | `screenshotTimeoutSec` | Promise.race + timeout |
| HTML 解析 | `parseTimeoutSec` | execa timeout |
| Page 关闭 | 5000ms | raceWith + timeoutRace |
| Context 关闭 | 10000ms | raceWith + timeoutRace |

**关键代码（超时信号）：**
```typescript
// packages/plugins/queue-restate/src/runner.ts:9-19
function timeoutSignal(timeoutSecs: number): AbortSignal {
  const controller = new AbortController();
  const timer = setTimeout(() => {
    controller.abort(new Error(`Job timed out after ${timeoutSecs} seconds`));
  }, timeoutSecs * 1000);
  return controller.signal;
}
```

### 5.3 资源泄漏防护

**Context 收割器（startContextReaper）：**
```typescript
// apps/workers/workers/crawlerWorker.ts:216-266
// 每 5 分钟检查一次，关闭超时的浏览器上下文
const maxContextAgeMs = (jobTimeoutSec + 30) * 1000 + 5 * 60_000;
setInterval(() => {
  for (const [id, entry] of activeContexts) {
    if (now - entry.createdAt > maxContextAgeMs) {
      // 强制关闭超时的 context
      raceWith(entry.context.close(), timeoutRace(10_000, () => false));
    }
  }
}, 5 * 60_000);
```

---

## 六、限流机制

### 6.1 域级限流

`apps/workers/workers/crawlerWorker.ts:2140-2188`

```typescript
async function checkDomainRateLimit(url: string, jobId: string): Promise<void> {
  const rateLimitResult = await rateLimitClient.checkRateLimit(
    {
      name: "domain-ratelimit",
      maxRequests: crawlerDomainRateLimitConfig.maxRequests,
      windowMs: crawlerDomainRateLimitConfig.windowMs,
    },
    hostname,
  );

  if (!rateLimitResult.allowed) {
    // 抖动策略：1.0 ~ 1.4 倍随机延迟，防止惊群效应
    const jitterFactor = 1.0 + Math.random() * 0.4;
    const delayMs = Math.floor(resetInSeconds * 1000 * jitterFactor);
    throw new QueueRetryAfterError(`Domain "${hostname}" is rate limited`, delayMs);
  }
}
```

**限流重试的特殊性：**
- 抛出 `QueueRetryAfterError` 而非普通 Error
- Dispatcher 收到后**不消耗重试次数**，仅睡眠后重试
- 实现：`dispatcher.ts:179-187`

---

## 七、重试机制与错误归因

### 7.1 重试策略配置

| 队列 | 最大重试次数 | 退避策略 |
|------|------------|---------|
| 抓取队列 | 5次 | 指数退避 + 完全抖动 |
| RPC 错误 | 5次 | 指数退避：5s → 10s → 20s → 40s → 60s |
| 普通错误 | 5次 | 固定 1s 延迟 |
| 限流错误 | 无限制 | 服务端指定延迟 + 抖动 |

**指数退避 + 完全抖动实现：**
```typescript
// packages/plugins/queue-restate/src/dispatcher.ts:170-172
const baseMs = Math.min(5000 * 2 ** runNumber, 60000);  // 上限 60s
const delayMs = Math.floor(ctx.rand.random() * baseMs);
await ctx.sleep(delayMs, "rpc error retry");
```

### 7.2 可重试 HTTP 状态码

`apps/workers/workers/crawlerWorker.ts:135-140`

```typescript
function shouldRetryCrawlStatusCode(statusCode: number | null): boolean {
  if (statusCode === null) return false;
  return statusCode === 403 || statusCode === 429 || statusCode >= 500;
}
```

### 7.3 错误类型分类

| 错误类型 | 处理方式 | 重试消耗 |
|---------|---------|---------|
| `QueueRetryAfterError` | 睡眠后重试 | 不消耗 |
| 403/429/5xx HTTP 状态 | 触发重试 | 消耗 1 次 |
| RPC 错误（服务不可用） | 指数退避重试 | 消耗 1 次 |
| 验证错误（Zod） | 直接标记完成 | 不重试 |
| 其他业务错误 | 1s 后重试 | 消耗 1 次 |

---

## 八、状态回写与失败追踪

### 8.1 数据库状态字段

`packages/db/schema.ts:249-277`

```sql
CREATE TABLE bookmarkLinks (
  id TEXT PRIMARY KEY,
  url TEXT NOT NULL,
  -- ... 元数据字段
  crawledAt TIMESTAMP,
  crawlStatus TEXT CHECK (crawlStatus IN ('pending', 'failure', 'success')) DEFAULT 'pending',
  crawlStatusCode INTEGER DEFAULT 200
);
```

### 8.2 成功路径状态流转

```
onComplete 回调 (crawlerWorker.ts:384-397)
  ↓
更新 bookmarkLinks.crawlStatus = 'success'
  ↓
触发下游任务（AI 标签、摘要、搜索索引）
```

### 8.3 失败路径状态流转

```
onError 回调 (crawlerWorker.ts:398-440)
  ↓
if (numRetriesLeft == 0)  // 彻底失败
  ↓
├─ 更新 bookmarkLinks.crawlStatus = 'failure'
├─ 清除 bookmarks.taggingStatus = null
├─ 清除 bookmarks.summarizationStatus = null
└─ 记录错误日志（含 stack trace）
```

### 8.4 可追踪线索

**1. 结构化事件日志（Event Log）**

`packages/shared-server/src/eventLogTypes.ts:1-9`

```typescript
{
  "event.name": "crawlerWorker.run",
  "job.id": "job_xxx",
  "job.run_number": 2,
  "crawler.url": "https://example.com",
  "crawler.domain": "example.com",
  "crawler.status_code": 503,
  "crawler.proxy": "http://proxy:8080"
}
```

**2. OpenTelemetry 分布式追踪**

通过 `withWorkerTracing` 和 `withSpan` 包装，每个任务生成完整的追踪链：
- `crawlerWorker.run` → 任务总览
- `crawlerWorker.crawlPage` → 页面抓取
  - `getBrowserInstance` → 浏览器实例获取
  - `createContext` → 上下文创建
  - `setupPage` → 页面配置
  - `validateNavigationTarget` → URL 验证
  - `navigate` → 页面导航
  - `waitForLoadState` → 等待加载
  - `captureAssets` → 资源捕获
    - `extractHtml` → HTML 提取
    - `captureScreenshot` → 截图
    - `capturePdf` → PDF
- `crawlerWorker.runParseSubprocess` → HTML 解析
- `crawlerWorker.storeScreenshot/Pdf` → 资源存储
- `crawlerWorker.downloadAndStoreFile` → 文件下载
- `crawlerWorker.archiveWebpage` → 整页归档

**3. Prometheus 指标**

- `crawlerStatusCodeCounter`：按状态码 + 代理统计
- `bookmarkCrawlLatencyHistogram`：首次抓取延迟分布
- `workerStatsCounter`：completed/failed/failed_permanent 计数

**4. Restate Journal**

Restate 原生保留 3 天的执行日志，可通过 Restate Dashboard 查看：
- 每次重试的输入输出
- 状态变迁历史
- 异常堆栈信息

---

## 九、对外通知机制

### 9.1 Webhook 触发

`apps/workers/workers/crawlerWorker.ts:2331-2339`

```typescript
const webhookService = new WebhooksService(db);
await webhookService.triggerWebhook(
  bookmarkId,
  "crawled",    // 操作类型
  userId,
  enqueueOpts,   // 继承优先级和 groupId
);
```

**触发时机：** 抓取成功后，在 `runCrawler` 函数末尾同步调用（异步入队）

### 9.2 下游任务级联

抓取成功自动触发的下游任务链：

```
抓取任务成功
  ├─ OpenAIQueue → 标签生成 (type: "tag")
  ├─ OpenAIQueue → 摘要生成 (type: "summarize")
  ├─ SearchIndexingQueue → 搜索索引重建
  ├─ VideoWorkerQueue → 视频下载（可选）
  ├─ WebhookQueue → Webhook 通知
  └─ AssetPreprocessingQueue → 资产预处理（PDF/图片类型）
```

---

## 十、关键设计总结

### 10.1 架构亮点

1. **插件化队列**：Queue 接口抽象，支持 Restate/Liteque 多种实现
2. **优先级 + 公平性**：组间轮询 + 组内优先级，避免单用户饿死
3. **多层超时**：从任务级到操作级的完整超时保护
4. **资源隔离**：HTML 解析在独立子进程，防止内存泄漏影响主进程
5. **可观测性**：Event Log + Tracing + Metrics 三位一体

### 10.2 失败处理哲学

- **尽早失败**：各阶段检查 abortSignal，快速终止
- **优雅降级**：截图/PDF 失败不影响主流程，只记录警告
- **状态一致性**：数据库事务保证 crawlStatus 与下游任务状态原子更新
- **可追溯性**：多维度追踪线索，便于事后排查

### 10.3 核心文件速查

| 功能模块 | 文件路径 |
|---------|---------|
| 队列定义 | `packages/shared-server/src/queues.ts` |
| 队列接口 | `packages/shared/queueing.ts` |
| Restate 分发器 | `packages/plugins/queue-restate/src/dispatcher.ts` |
| Restate 执行器 | `packages/plugins/queue-restate/src/runner.ts` |
| 优先级信号量 | `packages/plugins/queue-restate/src/semaphore.ts` |
| 抓取 Worker | `apps/workers/workers/crawlerWorker.ts` |
| Worker 入口 | `apps/workers/index.ts` |
| 任务入队 | `packages/trpc/routers/bookmarks.ts` |
| 数据库 Schema | `packages/db/schema.ts` |
| 事件类型定义 | `packages/shared-server/src/eventLogTypes.ts` |

---

*本分析基于 Karakeep 项目源码，生成时间：2026-05-30*
